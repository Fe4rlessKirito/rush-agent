Implement the previously approved low-risk view-state extraction, then separately lay groundwork for Bug Bounty Mode without adding active testing yet.

Part A - finish the current ChatPanel refactor:
1. Create `src/ui/chat/useChatPanelViewState.ts`.
   - Move derived values from the bottom of `ChatPanel.tsx`: picker conversations/runs, attachment accept type, active subagent selection, rendered chat slicing/grouping, and hidden count.
   - Keep the `MAX_RENDERED_MESSAGES = 80` behavior exactly.
2. Update `ChatPanel.tsx` to use the hook and remove now-unused imports/constants.
3. Run `npm run build` and fix any issues.

Part B - start Bug Bounty Mode foundation safely:
4. Add a persisted bug bounty store/model, likely `src/core/bugBountyStore.ts`.
   - Store program profiles only: name, platform/program URL, researcher handle, required headers, in-scope assets, out-of-scope assets, excluded vulnerability classes, notes, created/updated timestamps.
   - No scanning, no exploit execution, no HackerOne automation, no report submission.
5. Add pure policy parsing helpers, likely `src/core/bugBountyPolicy.ts`.
   - Parse pasted policy text into a draft program profile using conservative heuristics.
   - Extract obvious URLs/domains, required headers, out-of-scope/exclusion lines, and notes.
   - Keep it local and deterministic.
6. Add a basic UI view/component only if there is an existing navigation pattern that can be followed without broad app shell changes. If app-shell navigation is more involved, stop after store/helpers and report that UI wiring should be its own pass.
7. Run `npm run build` again and fix any TypeScript/build issues.

Safety constraints:
- This pass must not add autonomous target testing, mass scanning, credential attacks, DoS/load testing, stealth/evasion, or automatic HackerOne submission.
- The feature is an authorized program-profile and policy-management foundation only.