fastapi>=0.110
uvicorn[standard]>=0.29
httpx[socks]>=0.27
websockets>=12.0
playwright
cloakbrowser
python-multipart
json5
# fallback only (if cloakbrowser import fails the worker uses stock playwright):
# pip install playwright && playwright install chromium
